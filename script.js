function add(op1, op2)
{
    return op1 + op2;
}

function subtract(op1, op2)
{
    return op1 - op2;
}

function multiply(op1, op2)
{
    return op1 * op2;
}

function divide(op1, op2)
{
    if(op2 == 0)
    {
        //Run some error handling function for this
    }
    return op1 / op2;
}

function operate(op1, op2, operator)
{
    let result;
    switch(operator)
    {
        case '+':
            break;

        case '-':
            break;

        case '*':
            break;

        case '/':
            break;

        default:
            //Error handling function
    }

    //Print result to 'screen'
    //Store the result somewhere for future computation
}